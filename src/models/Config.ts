// Base Url
export const BASE_URL: string = "https://pokeapi.co/api/v2/";

// Continuos fetch of all pokemons endpoint
export const ENTIRE_POKEMON_ENTRIES: string = `${BASE_URL}pokemon`;

// Get a pokemon using id or name
export const SEARCH_A_POKEMON: string = `${BASE_URL}pokemon/`;

// DetailedPokemon Endpoint
export const DETAILED_POKEMON: string = `${BASE_URL}pokemon/`;

// Pagination pokemon list
export const PAGINATION_LIST: string = `${BASE_URL}pokemon/`;
